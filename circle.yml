#
# Build configuration for Circle CI
#

general:
  branches:
    ignore:
      - masters
      - /release\/.*/
      - /bugfix\/.*/
    only:
      - dev

deployment:
  develop:
    branch: dev
    commands:
      - ./deploy_dev.sh

